# softflowd+

A rewrite of [irino/softflowd](https://github.com/irino/softflowd) (a C
flow-monitoring / NetFlow-export daemon) into modern C++ with the
memory-safety practices.

## build

```
cmake -S . -B build
cmake --build build -j
```
